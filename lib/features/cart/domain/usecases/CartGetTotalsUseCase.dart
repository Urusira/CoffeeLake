import '../../../product/data/models/CartProductData.dart';
import '../repositories/CartRepository.dart';

class CartGetTotalsUseCase {
  CartRepository cartRepository;

  CartGetTotalsUseCase(this.cartRepository);

  Future<Map<String, double>> call() async {
    //TODO: ДОДЕЛАТЬ ГОВНО
    throw Exception("need realize todo!");
    фыва
  }
}